let nome = prompt('Oi!, como é o seu nome?');
alert(`Olá ${nome}, tudo bem`);

const num = Number(prompt('Informe um nome: '));

const antNum = (num - 1);
const sucNum = (num + 1);

alert(`O antecessor do seu número é ${antNum} e o sucessor é ${sucNum}`);